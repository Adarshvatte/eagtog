{
    "builds": [{
        "src": "eatog/wsgi.py",
        "use": "@vercel/python",
        "config": {
            "maxLambdaSize": "15mb",
            "runtime": "python3.9",
            "maxDuration": 30,
            "memory": 1700
        }
    }],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "eatog/wsgi.py"
        }
    ]
}
